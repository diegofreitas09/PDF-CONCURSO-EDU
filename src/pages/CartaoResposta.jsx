import React,{useState}from"react";
import{Download,ScanLine}from"lucide-react";
import AnswerSheetScanner from"../components/simulations/AnswerSheetScanner";
import{generateAnswerSheetPdf}from"../utils/answerSheet";
import"../styles/simulados.css";
export default function CartaoResposta(){const[read,setRead]=useState(null);return <section className="page sim-page"><div className="sim-heading"><div><div className="page-eyebrow">CARTÃO-RESPOSTA INTELIGENTE</div><h1>Gabarito OMR</h1><p>Baixe o cartão de 80 questões, preencha A–D e depois fotografe para conferir as marcações.</p></div><div className="sim-heading-icon"><ScanLine size={28}/></div></div><div className="sim-builder"><div className="sim-builder-head"><div><strong>Modelo SEDUC/CE 2026 · 80 questões</strong><span>4 alternativas por questão · A, B, C e D · marcadores de alinhamento para câmera</span></div></div><div className="sim-profile-note"><strong>Como usar:</strong> imprima em A4 sem redimensionar, preencha completamente uma única bolha por questão, fotografe a folha inteira e confira a leitura antes da correção.</div><div className="sim-builder-actions"><button className="sim-download" onClick={()=>generateAnswerSheetPdf()}><Download size={18}/> Baixar cartão-resposta PDF</button></div></div><AnswerSheetScanner questions={80} onApply={rows=>setRead(rows)}/>{read&&<div className="sim-builder"><strong>Leitura confirmada</strong><p>{read.filter(r=>r.selected!==null).length} respostas reconhecidas. A estrutura já está pronta para ser vinculada ao gabarito do simulado e gerar a correção automática.</p></div>}</section>}
