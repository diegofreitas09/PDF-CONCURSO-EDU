import React,{useMemo,useState}from"react";
import{ArrowLeft,ArrowRight,CheckCircle2,ClipboardList,Download,Play,RotateCcw,Trophy,ShieldCheck,SlidersHorizontal}from"lucide-react";
import{jsPDF}from"jspdf";
import{ALL_QUESTIONS}from"../data/questionSources";
import QuestionMedia from"../components/questions/QuestionMedia";
import"../styles/simulados.css";

const STORAGE_KEY="pdf-concurso-edu-state-v1";
const DRAW_KEY="pdf-concurso-edu-simulation-draw-v3";
const OFFICIAL={
 title:"SEDUC/CE 2026 — Professor de Filosofia",
 edital:"Edital nº 014/2026-SEDUC/SEPLAG",
 blocks:[
  {key:"ped",label:"Educação Brasileira: Temas Educacionais e Pedagógicos",count:8,min:3},
  {key:"adm",label:"Administração Pública",count:8,min:3},
  {key:"por",label:"Língua Portuguesa",count:8,min:3},
  {key:"ind",label:"Leitura e Interpretação de Dados e Indicadores Educacionais",count:6,min:2},
  {key:"esp",label:"Conhecimentos Específicos — Filosofia",count:50,min:20}
 ],p1Min:12,totalMin:40
};
function norm(v=""){return String(v).normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase()}
function officialKey(d=""){const s=norm(d);if(s.includes("administracao publica"))return"adm";if(s.includes("lingua portuguesa"))return"por";if(s.includes("indicador")||s.includes("interpretacao de dados"))return"ind";if(s.includes("filosofia"))return"esp";if(s.includes("educacao brasileira")||s.includes("pedagog"))return"ped";return null}
function shuffle(items){const a=[...items];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
function loadPlatform(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY))||{}}catch{return{}}}
function loadDrawState(){try{return JSON.parse(localStorage.getItem(DRAW_KEY))||{usedByDiscipline:{},lastExamSignature:""}}catch{return{usedByDiscipline:{},lastExamSignature:""}}}
function saveDrawState(value){localStorage.setItem(DRAW_KEY,JSON.stringify(value))}
function formatDuration(seconds=0){const s=Math.max(0,Math.round(seconds));const h=Math.floor(s/3600),m=Math.floor((s%3600)/60),sec=s%60;return h?`${h}h ${m}min ${sec}s`:m?`${m}min ${sec}s`:`${sec}s`}
function saveSimulation(result,mode){const s=loadPlatform();const now=new Date().toISOString();const answers=[...(s.answers||[]),...result.answers.map(a=>({...a,at:now,simulation:true}))];const history=[...(s.simulationHistory||[]),{id:Date.now(),at:now,type:mode,total:result.total,correct:result.correct,accuracy:result.accuracy,durationSeconds:result.durationSeconds,byDiscipline:result.byDiscipline,officialProfile:result.officialProfile,questionIds:result.answers.map(a=>a.questionId)}];localStorage.setItem(STORAGE_KEY,JSON.stringify({...s,answers,simulations:Number(s.simulations||0)+1,simulationHistory:history}));window.dispatchEvent(new Event("pdfedu-state"))}
function permuteOptions(question){const indexed=(question.options||[]).map((text,originalIndex)=>({text,originalIndex}));let mixed=shuffle(indexed);if(mixed.length>1&&mixed.every((item,i)=>item.originalIndex===i))mixed=[...mixed.slice(1),mixed[0]];const newAnswer=mixed.findIndex(item=>item.originalIndex===question.answer);return{...question,originalAnswer:question.answer,options:mixed.map(item=>item.text),answer:newAnswer,optionPermutation:mixed.map(item=>item.originalIndex),instanceId:`${question.id}-${Date.now()}-${Math.random().toString(36).slice(2,9)}`}}
function drawFromPool(pool,count,usedIds=[]){const used=new Set(usedIds.map(String));let fresh=shuffle(pool.filter(q=>!used.has(String(q.id))));const selected=[];while(selected.length<count){if(!fresh.length){used.clear();const inExam=new Set(selected.map(q=>String(q.id)));fresh=shuffle(pool.filter(q=>!inExam.has(String(q.id))));if(!fresh.length)break}const q=fresh.shift();if(!q)break;selected.push(q);used.add(String(q.id))}return{selected,usedIds:[...used]}}
function drawDisciplineQuestions(discipline,count,drawState){const pool=ALL_QUESTIONS.filter(q=>q.discipline===discipline);return drawFromPool(pool,count,drawState.usedByDiscipline?.[discipline]||[])}
function buildRandomExam(disciplines,counts){const previous=loadDrawState();const nextUsed={...(previous.usedByDiscipline||{})};let selected=[];for(const d of disciplines){const n=Number(counts[d]||0);if(!n)continue;const drawn=drawDisciplineQuestions(d,n,{usedByDiscipline:nextUsed});nextUsed[d]=drawn.usedIds;selected.push(...drawn.selected)}selected=selected.map(permuteOptions);let mixed=shuffle(selected);let signature=mixed.map(q=>String(q.id)).join("|");if(mixed.length>1&&signature===previous.lastExamSignature){mixed=[...mixed.slice(1),mixed[0]];signature=mixed.map(q=>String(q.id)).join("|")}saveDrawState({usedByDiscipline:nextUsed,lastExamSignature:signature});return mixed}
function buildOfficialExam(){const previous=loadDrawState();const nextUsed={...(previous.usedByDiscipline||{})};const exam=[];for(const block of OFFICIAL.blocks){const pool=ALL_QUESTIONS.filter(q=>officialKey(q.discipline)===block.key);const stateKey=`official-${block.key}`;const drawn=drawFromPool(pool,block.count,nextUsed[stateKey]||[]);if(drawn.selected.length<block.count)return{exam:[],error:`Banco insuficiente em ${block.label}: ${drawn.selected.length}/${block.count}.`};nextUsed[stateKey]=drawn.usedIds;exam.push(...drawn.selected.map(q=>permuteOptions({...q,officialBlock:block.key,officialLabel:block.label})))}saveDrawState({usedByDiscipline:nextUsed,lastExamSignature:exam.map(q=>q.id).join("|")});return{exam,error:""}}
function profileFromAnswers(rows){const blocks=OFFICIAL.blocks.map(b=>{const r=rows.filter(a=>a.officialBlock===b.key);const correct=r.filter(a=>a.correct).length;return{...b,total:r.length,correct,passed:correct>=b.min}});const p1=blocks.filter(b=>b.key!=="esp").reduce((s,b)=>s+b.correct,0);const p2=blocks.find(b=>b.key==="esp")?.correct||0;const total=p1+p2;return{blocks,p1,p2,total,p1Passed:p1>=OFFICIAL.p1Min,p2Passed:p2>=20,totalPassed:total>=OFFICIAL.totalMin,passed:blocks.every(b=>b.passed)&&p1>=OFFICIAL.p1Min&&p2>=20&&total>=OFFICIAL.totalMin}}

export default function Simulados(){
 const disciplines=useMemo(()=>[...new Set(ALL_QUESTIONS.map(q=>q.discipline).filter(Boolean))].sort(),[]);
 const available=useMemo(()=>Object.fromEntries(disciplines.map(d=>[d,ALL_QUESTIONS.filter(q=>q.discipline===d).length])),[disciplines]);
 const officialAvailability=useMemo(()=>Object.fromEntries(OFFICIAL.blocks.map(b=>[b.key,ALL_QUESTIONS.filter(q=>officialKey(q.discipline)===b.key).length])),[]);
 const officialReady=OFFICIAL.blocks.every(b=>(officialAvailability[b.key]||0)>=b.count);
 const [mode,setMode]=useState("official");const[counts,setCounts]=useState({});const[exam,setExam]=useState([]);const[answers,setAnswers]=useState({});const[index,setIndex]=useState(0);const[finished,setFinished]=useState(false);const[saved,setSaved]=useState(false);const[startedAt,setStartedAt]=useState(null);const[finishedAt,setFinishedAt]=useState(null);const[pdfLoading,setPdfLoading]=useState(false);const[pdfError,setPdfError]=useState("");const[builderError,setBuilderError]=useState("");
 const totalRequested=Object.values(counts).reduce((a,b)=>a+(Number(b)||0),0);
 function resetRun(selected,selectedMode){if(!selected.length)return;setMode(selectedMode);setExam(selected);setAnswers({});setIndex(0);setFinished(false);setSaved(false);setStartedAt(Date.now());setFinishedAt(null);setPdfError("");setBuilderError("")}
 function setCount(d,value){const n=Math.max(0,Math.min(Number(value)||0,available[d]||0));setCounts(c=>({...c,[d]:n}))}
 function generateCustom(){resetRun(buildRandomExam(disciplines,counts),"custom")}
 function generateOfficial(){const built=buildOfficialExam();if(built.error){setBuilderError(built.error);return}resetRun(built.exam,"official")}
 function choose(i){if(!finished)setAnswers(a=>({...a,[exam[index].instanceId]:i}))}
 function buildResult(endTime=finishedAt||Date.now()){const answerRows=exam.map(q=>({questionId:q.id,instanceId:q.instanceId,discipline:q.discipline,topic:q.topic,officialBlock:q.officialBlock,officialLabel:q.officialLabel,selected:answers[q.instanceId],answer:q.answer,correct:answers[q.instanceId]===q.answer,optionPermutation:q.optionPermutation}));const correct=answerRows.filter(a=>a.correct).length;const byDiscipline={};answerRows.forEach(a=>{const label=a.officialLabel||a.discipline;byDiscipline[label]??={total:0,correct:0};byDiscipline[label].total++;if(a.correct)byDiscipline[label].correct++});const durationSeconds=startedAt?Math.max(0,Math.round((endTime-startedAt)/1000)):0;return{answers:answerRows,total:exam.length,correct,accuracy:exam.length?Math.round(correct/exam.length*100):0,byDiscipline,durationSeconds,officialProfile:mode==="official"?profileFromAnswers(answerRows):null}}
 function finish(){if(!exam.length)return;const end=Date.now();setFinishedAt(end);setFinished(true);const result=buildResult(end);saveSimulation(result,mode);setSaved(true);window.scrollTo({top:0,behavior:"smooth"})}
 async function downloadReport(){if(!finished||!exam.length)return;setPdfLoading(true);setPdfError("");try{const doc=new jsPDF({unit:"mm",format:"a4"});const result=buildResult();const platform=loadPlatform();const name=platform?.settings?.name||"Aluno";const date=new Date(finishedAt||Date.now());const margin=16,pageW=210,pageH=297,usable=pageW-margin*2;let y=18;const ensure=(need=12)=>{if(y+need>pageH-16){doc.addPage();y=18}};const addText=(text,size=10,bold=false,space=4)=>{doc.setFont("helvetica",bold?"bold":"normal");doc.setFontSize(size);const lines=doc.splitTextToSize(String(text||""),usable);ensure(lines.length*(size*.38)+space);doc.text(lines,margin,y);y+=lines.length*(size*.38)+space};doc.setFillColor(20,31,48);doc.rect(0,0,210,38,"F");doc.setTextColor(255,255,255);doc.setFont("helvetica","bold");doc.setFontSize(20);doc.text("PDF CONCURSO EDU",margin,17);doc.setFontSize(12);doc.text(mode==="official"?"Relatório — Simulado Oficial SEDUC/CE 2026":"Relatório de Simulado Personalizado",margin,27);doc.setTextColor(20,31,48);y=48;addText(`Aluno: ${name}`,11,true,2);addText(`Data: ${date.toLocaleDateString("pt-BR")} às ${date.toLocaleTimeString("pt-BR",{hour:"2-digit",minute:"2-digit"})}`,10,false,2);addText(`Duração: ${formatDuration(result.durationSeconds)}`,10,false,5);doc.setFillColor(245,247,250);doc.roundedRect(margin,y,usable,28,3,3,"F");doc.setFont("helvetica","bold");doc.setFontSize(16);doc.text(`${result.correct}/${result.total} acertos`,margin+6,y+10);doc.setFontSize(12);doc.text(`Desempenho geral: ${result.accuracy}%`,margin+6,y+20);y+=36;addText("Desempenho por disciplina",13,true,4);Object.entries(result.byDiscipline).forEach(([d,r])=>addText(`${d}: ${r.correct}/${r.total} acertos — ${Math.round(r.correct/r.total*100)}%`,10,false,2));if(result.officialProfile){const p=result.officialProfile;y+=4;addText("Comparação com os perfis mínimos do Edital nº 014/2026",13,true,4);p.blocks.forEach(b=>addText(`${b.passed?"ATINGIU":"NÃO ATINGIU"} — ${b.label}: ${b.correct}/${b.total} | mínimo ${b.min}`,9.5,true,2));addText(`${p.p1Passed?"ATINGIU":"NÃO ATINGIU"} — P1: ${p.p1}/30 | mínimo 12`,9.5,true,2);addText(`${p.p2Passed?"ATINGIU":"NÃO ATINGIU"} — P2: ${p.p2}/50 | mínimo 20`,9.5,true,2);addText(`${p.totalPassed?"ATINGIU":"NÃO ATINGIU"} — Total P1+P2: ${p.total}/80 | mínimo 40`,9.5,true,3);addText(`Situação pelos perfis do edital: ${p.passed?"APROVADO NOS PERFIS MÍNIMOS":"ABAIXO DE UM OU MAIS PERFIS MÍNIMOS"}`,11,true,5)}y+=4;addText("Detalhamento das questões",13,true,5);exam.forEach((q,i)=>{const selected=answers[q.instanceId],ok=selected===q.answer;ensure(32);doc.setDrawColor(ok?70:190,ok?150:70,ok?90:70);doc.setLineWidth(.5);doc.line(margin,y,margin+usable,y);y+=5;addText(`Questão ${i+1} — ${q.officialLabel||q.discipline} · ${q.topic||"Sem tópico"}`,11,true,3);if(q.context){addText("TEXTO-BASE",9,true,2);addText(q.context,8.5,false,3)}if(q.media)addText("[QUESTÃO COM APOIO VISUAL — consulte a versão interativa da plataforma]",8.5,true,3);addText(q.statement,10,false,3);q.options.forEach((o,oi)=>{const letter=String.fromCharCode(65+oi);let prefix=`${letter}) `;if(oi===selected)prefix+="[MARCADA] ";if(oi===q.answer)prefix+="[GABARITO] ";addText(prefix+o,9,false,2)});addText(`Resposta: ${selected===undefined?"Não respondida":String.fromCharCode(65+selected)} | Gabarito: ${String.fromCharCode(65+q.answer)} | ${ok?"ACERTOU":"ERROU"}`,9.5,true,3);if(q.explanation)addText(`Comentário: ${q.explanation}`,8.5,false,4)});ensure(20);doc.setDrawColor(180);doc.line(margin,y,margin+usable,y);y+=6;addText("Relatório gerado automaticamente pela plataforma PDF Concurso EDU.",8,false,2);if(mode==="official")addText("Estrutura e perfis mínimos conforme o Edital nº 014/2026-SEDUC/SEPLAG, Anexo II.",8,false,2);doc.save(`Relatorio_Simulado_${mode==="official"?"SEDUC2026":"Personalizado"}_${date.toISOString().slice(0,10)}.pdf`)}catch(e){console.error("Erro ao gerar PDF",e);setPdfError(`Não foi possível gerar o PDF: ${e?.message||"erro desconhecido"}`)}finally{setPdfLoading(false)}}
 const result=finished?buildResult():null;const current=exam[index];
 if(!exam.length)return <section className="page sim-page"><div className="sim-heading"><div><div className="page-eyebrow">GERADOR DE SIMULADOS</div><h1>Simulados</h1><p>Escolha entre montar livremente ou fazer uma prova aleatória com a estrutura oficial do concurso.</p></div><div className="sim-heading-icon"><ClipboardList size={28}/></div></div><div className="sim-mode-grid"><button className={`sim-mode-card ${mode==="official"?"active":""}`} onClick={()=>setMode("official")}><ShieldCheck size={24}/><strong>Simulado oficial SEDUC 2026</strong><span>80 questões na ordem e quantidade do edital, com análise dos perfis mínimos.</span></button><button className={`sim-mode-card ${mode==="custom"?"active":""}`} onClick={()=>setMode("custom")}><SlidersHorizontal size={24}/><strong>Criar meu simulado</strong><span>Escolha livremente as disciplinas e a quantidade de questões.</span></button></div>{mode==="official"?<div className="sim-builder sim-official-builder"><div className="sim-builder-head"><div><strong>{OFFICIAL.title}</strong><span>{OFFICIAL.edital} · P1 30 + P2 50 = 80 questões</span></div><div className="sim-total"><span>Total</span><strong>80</strong></div></div><div className="sim-official-table">{OFFICIAL.blocks.map(b=><div className="sim-official-row" key={b.key}><div><strong>{b.label}</strong><span>{officialAvailability[b.key]||0} questões no banco</span></div><b>{b.count} questões</b><em>mín. {b.min} acertos</em></div>)}</div><div className="sim-profile-note"><strong>Perfis adicionais:</strong> P1 mínimo 12/30 · P2 mínimo 20/50 · total mínimo 40/80. É preciso cumprir todos os perfis.</div>{builderError&&<div className="sim-builder-error">{builderError}</div>}<div className="sim-builder-actions"><button type="button" className="sim-generate" disabled={!officialReady} onClick={generateOfficial}><Play size={18}/> Montar prova oficial aleatória</button></div></div>:<div className="sim-builder"><div className="sim-builder-head"><div><strong>Crie seu simulado</strong><span>{ALL_QUESTIONS.length} questões auditadas disponíveis · sorteio rotativo</span></div><div className="sim-total"><span>Total escolhido</span><strong>{totalRequested}</strong></div></div><div className="sim-discipline-list">{disciplines.map(d=><div className="sim-discipline-row" key={d}><div><strong>{d}</strong><span>{available[d]} disponíveis</span></div><input type="number" min="0" max={available[d]} value={counts[d]||""} placeholder="0" onChange={e=>setCount(d,e.target.value)}/></div>)}</div><div className="sim-builder-actions"><button type="button" className="sim-clear" onClick={()=>setCounts({})}><RotateCcw size={17}/> Limpar</button><button type="button" className="sim-generate" disabled={!totalRequested} onClick={generateCustom}><Play size={18}/> Sortear simulado ({totalRequested})</button></div></div>}</section>;
 if(finished)return <section className="page sim-page"><div className="sim-result-hero"><Trophy size={34}/><div><div className="page-eyebrow">RESULTADO DO SIMULADO</div><h1>{result.correct}/{result.total} acertos</h1><p>Desempenho geral: <strong>{result.accuracy}%</strong> · Tempo: <strong>{formatDuration(result.durationSeconds)}</strong>{saved?" · resultado salvo no Dashboard":""}</p></div></div>{result.officialProfile&&<div className={`sim-official-verdict ${result.officialProfile.passed?"passed":"failed"}`}><ShieldCheck size={24}/><div><strong>{result.officialProfile.passed?"Perfis mínimos atingidos":"Atenção aos perfis mínimos"}</strong><span>{result.officialProfile.passed?"Você atingiu todos os mínimos eliminatórios do edital neste simulado.":"Pelo menos um dos mínimos eliminatórios do edital não foi atingido."}</span></div></div>}<div className="sim-report-actions"><button className="sim-download" onClick={downloadReport} disabled={pdfLoading}><Download size={18}/>{pdfLoading?"Gerando relatório...":"Baixar relatório completo em PDF"}</button>{pdfError&&<span className="sim-pdf-error">{pdfError}</span>}</div><div className="sim-result-grid">{Object.entries(result.byDiscipline).map(([d,r])=><div className="sim-result-card" key={d}><strong>{d}</strong><span>{r.correct}/{r.total} acertos</span><b>{Math.round(r.correct/r.total*100)}%</b></div>)}</div>{result.officialProfile&&<div className="sim-profile-grid">{result.officialProfile.blocks.map(b=><div className={`sim-profile-card ${b.passed?"passed":"failed"}`} key={b.key}><strong>{b.label}</strong><b>{b.correct}/{b.total}</b><span>Mínimo: {b.min} · {b.passed?"ATINGIU":"NÃO ATINGIU"}</span></div>)}<div className={`sim-profile-card ${result.officialProfile.p1Passed?"passed":"failed"}`}><strong>Total P1</strong><b>{result.officialProfile.p1}/30</b><span>Mínimo: 12</span></div><div className={`sim-profile-card ${result.officialProfile.totalPassed?"passed":"failed"}`}><strong>Total P1 + P2</strong><b>{result.officialProfile.total}/80</b><span>Mínimo: 40</span></div></div>}<div className="sim-review-list">{exam.map((q,i)=>{const selected=answers[q.instanceId],ok=selected===q.answer;return <article className={`sim-review ${ok?"ok":"wrong"}`} key={q.instanceId}><header><strong>Questão {i+1}</strong><span>{q.officialLabel||q.discipline} · {q.topic}</span></header>{q.context&&<div className="sim-context">{q.context}</div>}<QuestionMedia media={q.media}/><p>{q.statement}</p><div className="sim-review-answer"><span>Você marcou: {selected===undefined?"Não respondida":String.fromCharCode(65+selected)}</span><strong>Gabarito: {String.fromCharCode(65+q.answer)}</strong></div>{q.explanation&&<small>{q.explanation}</small>}</article>})}</div><button className="sim-new" onClick={()=>{setExam([]);setFinished(false);setAnswers({});setStartedAt(null);setFinishedAt(null)}}><RotateCcw size={17}/> Novo simulado</button></section>;
 const answeredCount=Object.keys(answers).length;
 return <section className="page sim-page"><div className="sim-running-head"><button onClick={()=>setExam([])}><ArrowLeft size={17}/> Sair</button><div><strong>{mode==="official"?"Simulado oficial SEDUC 2026":"Simulado personalizado"}</strong><span>{answeredCount}/{exam.length} respondidas</span></div><button className="sim-finish" onClick={finish}>Finalizar</button></div><article className="sim-question-card"><header><div><strong>Questão {index+1} de {exam.length}</strong><span>{current.officialLabel||current.discipline} · {current.topic}</span></div><span>{Math.round(((index+1)/exam.length)*100)}%</span></header>{current.context&&<div className="sim-context"><b>TEXTO-BASE</b><p>{current.context}</p></div>}<QuestionMedia media={current.media}/><div className="sim-statement">{current.statement}</div><div className="sim-options">{current.options.map((o,i)=><button type="button" key={`${current.instanceId}-${i}`} onClick={()=>choose(i)} className={answers[current.instanceId]===i?"selected":""}><span>{String.fromCharCode(65+i)}</span><p>{o}</p>{answers[current.instanceId]===i&&<CheckCircle2 size={19}/>}</button>)}</div><div className="sim-nav"><button disabled={index===0} onClick={()=>setIndex(i=>Math.max(0,i-1))}><ArrowLeft size={17}/> Anterior</button><div>{exam.map((q,i)=><button key={q.instanceId} className={`${i===index?"current":""} ${answers[q.instanceId]!==undefined?"answered":""}`} onClick={()=>setIndex(i)}>{i+1}</button>)}</div><button disabled={index===exam.length-1} onClick={()=>setIndex(i=>Math.min(exam.length-1,i+1))}>Próxima <ArrowRight size={17}/></button></div></article></section>
}
